# 115-1 專題演講　專案維護說明

國立臺灣海洋大學 食品科學系　**專題演講（一）** B32030EI
授課教師：陳建利、劉修銘　｜　開課班級：食品科學系三年級
時間：每週二 第八、九節 15:10–17:00　｜　地點：食品科學館 系演講廳（MFS001）　｜　每場 50 分鐘

這個專案有兩條主線：**(A) 邀請系上老師來演講**、**(B) 維護對外公開的排程網頁**。

---

## A. 邀請流程

### 現況

| 批次 | 對象 | 狀態 |
|---|---|---|
| 第一批 | 孫寶年、鄭森雄、林家媛 | 8/19 已寄出。林家媛老師已回覆確認 **9/8 16:00**。 |
| 第二批 | 其餘 24 位 | 其中 14 位的 Gmail 草稿已備妥待寄，10 位尚未建草稿（見 `roster.csv` 的「狀態」欄）。 |

**尚未建草稿的 10 位**：廖若川、張君如、陳冠文、蔡博崴、陳建利、陳詠宗、王上達、吳俊逸、吳奕賢、洪悅豪。

### 名單依據

`roster.csv` 是唯一名單來源，含姓名、職稱、**信件稱謂**、收件信箱、副本、批次、狀態、已排時段、備註。

名單是以食科系網站「專任師資」頁為準，扣掉劉修銘本人與孫寶年（另列講座教授），再加上兩位榮譽講座教授孫寶年、鄭森雄，共 **27 位**。

- **洪悅豪老師**為 115/8/1 新聘（專長：發酵工程、應用微生物、生物精煉、海藻化學），今年首度納入。
- **柯源悌老師已退休**，經確認本學期不邀請。（注意：生科院的行政群組名單裡 `irisko@` 可能還在，那是行政名單未更新，不是還在職。）

### 信件稱謂的規矩

依劉老師往年慣例：資深者用「姓＋老師」，年輕者用「名＋老師」。特例：
- 吳彰哲 → **院長**（生科院院長）
- 陳泰源 → **主任**（系主任）
- 兩位榮譽講座教授 → **敬愛的◯◯◯講座教授**，且自稱**「學生劉修銘 敬上」**
- 方翠筠 → **翠筠老師**（避免與方銘志老師的「方老師」混淆）

### 信件模板

見 `templates/`：講座教授版、一般教師版、林家媛老師版、洪悅豪老師版（新聘）、確認回信版，以及共用的時段表與簽名檔。

### 幾個時間點

- **回覆期限 8/28（五）**
- **9/1（二）前**把確定的排程回覆給所有老師（此句寫在一般教師版信裡；**講座教授與林家媛老師那三封刻意不寫**，因為她們的時段一收到就立刻安排）
- 9/8（二）第一堂課

### 收到回覆時要做的事

1. 回信確認（`templates/05_確認回信版.md`）
2. 更新 `data.json`：`slots` 填入時段、`invitees` 該位老師的 `status` 改 `confirmed`、`slot` 填時間
3. `python3 build.py` 後 commit、push（見下方 B）
4. 若尚有未寄出的信，把時段表裡被選走的格子標上姓名再寄，避免撞號

---

## B. 公開網頁

GitHub repo：`HMLiu-NTOU/ntou-fs-seminar-115-1`（GitHub Pages，main / root）

| 檔案 | 用途 | 網址 |
|---|---|---|
| `index.html` | **學生版**：課程說明＋排程（含講者研究專長） | https://hmliu-ntou.github.io/ntou-fs-seminar-115-1/ |
| `t-f37b3eb9d27c.html` | **完整版**：另含 27 位老師的邀請狀態，**不含 email** | https://hmliu-ntou.github.io/ntou-fs-seminar-115-1/t-f37b3eb9d27c.html |

完整版的檔名是隨機字串，等同「不公開連結」，只給共同授課的陳建利老師。**不要改檔名，也不要從 index.html 連過去。**

### 唯一要編輯的檔案：`data.json`

**絕對不要直接改 `.html`** — 它們是產生出來的，下次 build 會被覆蓋。

```bash
python3 build.py
```

產生三個檔：

| 檔案 | 說明 |
|---|---|
| `index.html` | 學生版（要 commit） |
| `t-f37b3eb9d27c.html` | 完整版公開（要 commit） |
| `_internal_full.html` | 完整版**含 email**（已列入 `.gitignore`，**不要 commit**；要寄給陳建利老師時用這份） |

`build.py` 內建檢查：公開版若混進 email 會直接 assert 失敗。

### data.json 怎麼改

**填入一位確定的講者**（`a` = 15:10–16:00，`b` = 16:05–16:55）：

```json
{"date":"09/15","wk":2,
 "a":{"type":"filled","name":"孫寶年","rank":"榮譽講座教授",
      "field":"食品化學、水產加工化學","note":"（選填，例如「16:00 開始」）"},
 "b":{}}
```

同時把 `invitees` 裡該位老師的 `status` 改成 `"confirmed"`、`slot` 填 `"9/15　15:10"`。

`field`（研究專長）會顯示在學生版，幫同學找實驗室，請盡量填，可從系網站專任師資頁查。

**狀態值**：`confirmed` 已回覆確認｜`sent` 已寄出待回覆｜`draft` 草稿已備待寄｜`todo` 待建草稿
（若要新增 `declined`，記得同步在 `build.py` 的 `LABEL` 補一個標籤。）

每次更新都要改 `course.updated`。

### 發布

```bash
python3 build.py
git add -A
git commit -m "更新排程：<簡述>"
git push
```

GitHub Pages 約 1–3 分鐘生效。

---

## 排程規則（別弄錯）

- 可用的週二：9/8、9/15、9/22、9/29、10/6、10/13、10/20、11/3、11/10、11/17、11/24、12/1、12/8、12/15
- **10/27 為期中考試週（10/26–30）、12/22 為期末考試週（12/21–25）**，不排演講，`slots` 用 `"exam"` 欄位標示
- 9/8 第八節固定為**課程介紹**，第九節已由**林家媛老師**確認（她的課與本課程衝堂，只有第一週可以，時間 16:00 起，可彈性調整）
- 因此**可排時段共 26 格**，對上第二批的 24 位＋兩位講座教授，剛好滿檔沒有餘裕。若劉老師本人也要做實驗室介紹，需要再擠一格出來。
- 學期依據：115 學年度第 1 學期 9/7 開始上課、1/31 結束（海大行事曆）
